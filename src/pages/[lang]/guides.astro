---
// src/pages/[lang]/guides.astro
import StaticPage from '../../layouts/StaticPage.astro';
import { languages, type SupportedLanguage } from '../../i18n/ui';
import { useTranslations, useTranslatedPath } from '../../i18n/utils';

export function getStaticPaths() {
  return (Object.keys(languages) as SupportedLanguage[])
    .filter((l) => l !== 'en')
    .map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params as { lang: SupportedLanguage };
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);
const pageUrl = `https://onscicalc.com/${lang}/guides`;

const guideSchema = {
  "@context": "https://schema.org",
  "@type": "TechArticle",
  "@id": `${pageUrl}/#article`,
  "headline": t('guides.title'),
  "description": t('guides.desc'),
  "url": pageUrl,
  "inLanguage": lang,
  "datePublished": "2026-07-04T00:00:00Z",
  "dateModified": "2026-09-01T00:00:00Z",
  "author": {
    "@type": "Organization",
    "@id": "https://onscicalc.com/#organization"
  },
  "publisher": {
    "@type": "Organization",
    "@id": "https://onscicalc.com/#organization"
  },
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": pageUrl
  },
  "image": "https://onscicalc.com/og-image.png"
};
---

<StaticPage
  title={t('guides.title')}
  description={t('guides.desc')}
  canonical={pageUrl}
  ogType="article"
  publishedTime="2026-07-04T00:00:00Z"
  modifiedTime="2026-09-01T00:00:00Z"
  breadcrumbs={[{ name: t('nav.guides'), url: `/${lang}/guides` }]}
  jsonLd={guideSchema}
  lang={lang}
>

  <article class="prose-custom">
    <h1 class="text-2xl md:text-3xl font-bold text-ink font-geist tracking-tight mb-2">{t('guide.calc_guide')}</h1>
    <p class="text-sm text-ink/50 font-geist mb-8">{t('guides.desc')}</p>

    <div class="space-y-8 text-sm leading-relaxed text-ink/70 font-geist">

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.precedence_title')}</h2>
        <p>{t('guide.precedence_desc')}</p>
        <div class="bg-canvas-soft border border-hairline p-4 rounded-md font-geist-mono text-xs space-y-1 mt-3">
          <div>{t('guide.p1')} <span class="text-ink/50">( ... )</span></div>
          <div>{t('guide.p2')} <span class="text-ink/50">x^y</span></div>
          <div>{t('guide.p3')} <span class="text-ink/50">sin, cos, tan, log, ln</span></div>
          <div>{t('guide.p4')} <span class="text-ink/50">*, /</span></div>
          <div>{t('guide.p5')} <span class="text-ink/50">+, -</span></div>
        </div>
      </section>

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.trig_title')}</h2>
        <p>{t('guide.trig_desc')}</p>
        <div class="bg-canvas-soft-2 border border-hairline rounded-md p-3 mt-3 text-xs">
          <span class="font-semibold text-ink">💡 {t('guide.shift_tip')}</span>
        </div>
      </section>

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.exponent_title')}</h2>
        <p>{t('guide.exponent_desc')}</p>
      </section>

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.log_title')}</h2>
        <p>{t('guide.log_desc')}</p>
      </section>

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.factorial_title')}</h2>
        <p>{t('guide.factorial_desc')}</p>
      </section>

      <section>
        <h2 class="text-lg font-semibold text-ink mb-3">{t('guide.keyboard_title')}</h2>
        <p>{t('guide.keyboard_desc')}</p>
      </section>

      <section class="pt-4 border-t border-hairline">
        <p>
          <a href={translatePath('/')} class="text-brand-link font-medium hover:underline">← {t('nav.calculator')}</a>
        </p>
      </section>

    </div>
  </article>

</StaticPage>
